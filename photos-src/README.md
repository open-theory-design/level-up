# Drop raw exercise images here

Save the images you shared into **this** folder (`photos-src/`). Any filenames
are fine — but if you can, name them by exercise id so I don't have to guess:

| Exercise id      | Image you shared                                   |
|------------------|----------------------------------------------------|
| `lat_stretch`    | kneeling, forearms on a stool                      |
| `pec_stretch`    | standing at a doorframe (back view)                |
| `neck_stretch`   | seated cross-legged, hand pulling head to the side |
| `wall_angels`    | standing against a wall, arms up in a "W"          |
| `chin_tucks`     | side profile, hand on chin (gym)                   |

Then tell me they're in, and I'll crop each one to focus on the person and
normalize them all to the same size, writing the results to `../photos/<id>.jpg`
(which the app's **Photos** image style loads automatically).
